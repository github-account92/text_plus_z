# config for the "old" data preprocessed by Julius
# NOTE that this data is already normalized to per-example mean 0, std 1
# This means you should *not* use thresholding with this data!
# NOTE also that csv_path corpus_new is *not* a typo.
csv_path,/data/corpus_new.csv
array_dir,/data/cache/English_old
vocab_path,/data/vocab
data_type,mel
n_freqs,128
window_size,512
hop_length,128
normalize,True
keep_phase,False
resamle_rate,0