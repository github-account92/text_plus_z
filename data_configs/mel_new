# even though it's called 'new' it should be considered old. In particular, the
# data is not normalized and on a dB scale (10*log10) whereas the newer data is
# just on a (natural) log scale
csv_path,/data/corpus_new.csv
array_dir,/data/cache/English_new
vocab_path,/data/vocab
data_type,mel
n_freqs,128
window_size,400
hop_length,160
normalize,False
keep_phase,False
resample_rate,0